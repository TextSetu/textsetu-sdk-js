// This file is auto-generated by @hey-api/openapi-ts

import type { Options as ClientOptions, TDataShape, Client } from '@hey-api/client-fetch';
import type { ListFormatsData, ListFormatsResponse, ListProjectsData, ListProjectsResponse, GetProjectData, GetProjectResponse, ListLanguagesData, ListLanguagesResponse, AddLanguageData, AddLanguageResponse, GetStatsData, GetStatsResponse, ListKeysData, ListKeysResponse, CreateKeyData, CreateKeyResponse, DeleteKeyData, DeleteKeyResponse, GetKeyData, GetKeyResponse, UpdateKeyData, UpdateKeyResponse, UpsertKeyValueData, UpsertKeyValueResponse, ImportSourcesData, ImportSourcesResponse, ExportTranslationsData, ExportTranslationsResponse, ListBranchesData, ListBranchesResponse, CreateBranchData, CreateBranchResponse, DeleteBranchData, DeleteBranchResponse, GetBranchData, GetBranchResponse, ListBranchConflictsData, ListBranchConflictsResponse, ResolveBranchConflictsData, ResolveBranchConflictsResponse, StartBranchMergeData, StartBranchMergeResponse, CompleteBranchMergeData, CompleteBranchMergeResponse, LookupGlossaryTermsData, LookupGlossaryTermsResponse, CheckGlossaryData, CheckGlossaryResponse, ListGlossariesData, ListGlossariesResponse, CreateGlossaryData, CreateGlossaryResponse, DeleteGlossaryData, DeleteGlossaryResponse, GetGlossaryData, GetGlossaryResponse, UpdateGlossaryData, UpdateGlossaryResponse, ListGlossaryLanguagesData, ListGlossaryLanguagesResponse, AddGlossaryLanguageData, AddGlossaryLanguageResponse, ListGlossaryTermsData, ListGlossaryTermsResponse, CreateGlossaryTermData, CreateGlossaryTermResponse, DeleteGlossaryTermData, DeleteGlossaryTermResponse, GetGlossaryTermData, GetGlossaryTermResponse, UpdateGlossaryTermData, UpdateGlossaryTermResponse, SetGlossaryTermTranslationData, SetGlossaryTermTranslationResponse, GetTmMatchesData, GetTmMatchesResponse, SearchTmData, SearchTmResponse, PretranslateData, PretranslateResponse, GetPretranslateJobData, GetPretranslateJobResponse, ListTranslationMemoriesData, ListTranslationMemoriesResponse, CreateTranslationMemoryData, CreateTranslationMemoryResponse, DeleteTranslationMemoryData, DeleteTranslationMemoryResponse, GetTranslationMemoryData, GetTranslationMemoryResponse, UpdateTranslationMemoryData, UpdateTranslationMemoryResponse, ListTmLanguagesData, ListTmLanguagesResponse, AddTmLanguageData, AddTmLanguageResponse, ListTmEntriesData, ListTmEntriesResponse, CreateTmEntryData, CreateTmEntryResponse, DeleteTmEntryData, DeleteTmEntryResponse, GetTmEntryData, GetTmEntryResponse, UpdateTmEntryData, UpdateTmEntryResponse, SetTmEntryTranslationData, SetTmEntryTranslationResponse, GetAiEngineData, GetAiEngineResponse, AiTranslateData, AiTranslateResponse, GetAiTranslateJobData, GetAiTranslateJobResponse, ListWebhookEventTypesData, ListWebhookEventTypesResponse, ListWebhooksData, ListWebhooksResponse, CreateWebhookData, CreateWebhookResponse, DeleteWebhookData, DeleteWebhookResponse, GetWebhookData, GetWebhookResponse, UpdateWebhookData, UpdateWebhookResponse, RotateWebhookSecretData, RotateWebhookSecretResponse, SendWebhookTestData, SendWebhookTestResponse, ListWebhookDeliveriesData, ListWebhookDeliveriesResponse, GetWebhookDeliveryData, GetWebhookDeliveryResponse, ReplayWebhookDeliveryData, ReplayWebhookDeliveryResponse, GetContentManifestData, GetContentBlobData, ListPublicPlansData, ListPublicPlansResponse } from './types.gen.js';
import { client as _heyApiClient } from './client.gen.js';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean> = ClientOptions<TData, ThrowOnError> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * List supported formats
 * List every import/export file format the platform supports, with its id, name, file extensions, whether it can carry multiple languages in one file, and whether it can represent plural categories. Use the returned `id` as the `format` in import/export calls — see the [file formats reference](/docs/reference/file-formats). Requires the `read` scope; PATs always qualify.
 */
export const listFormats = <ThrowOnError extends boolean = false>(options?: Options<ListFormatsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListFormatsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/formats',
        ...options
    });
};

/**
 * List projects this token can read (paginated)
 * List every project the token can read, newest-name-first, so a client can offer a project picker without being told an id up front. A project token returns exactly its one bound project; a PAT returns the projects its owner can read, narrowed by any resource restriction on the token.
 *
 * **Constraints:**
 * - Requires the `translation_read` permission — a token without it gets 403, not an empty list.
 * - A project the token cannot read is omitted, never reported as forbidden.
 */
export const listProjects = <ThrowOnError extends boolean = false>(options?: Options<ListProjectsData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListProjectsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects',
        ...options
    });
};

/**
 * Get project metadata
 * Fetch a project's core settings — name, source language, key separator, and whether approval is required. Useful for resolving the key separator before creating keys, or the source language before exporting.
 */
export const getProject = <ThrowOnError extends boolean = false>(options: Options<GetProjectData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetProjectResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}',
        ...options
    });
};

/**
 * List project languages
 * List the languages configured on the project, including which one is the source language. Each entry resolves its display label from the org language catalog.
 */
export const listLanguages = <ThrowOnError extends boolean = false>(options: Options<ListLanguagesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListLanguagesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/languages',
        ...options
    });
};

/**
 * Add a language (manage)
 * Add a language to the project. Standard codes resolve to the shared global language; a non-standard code is created as an org custom language. Requires the `manage` scope — adding a language is a project-management operation, above ordinary `write`. PATs need the PROJECT_MANAGE permission — see [roles & permissions](/docs/guides/roles-and-permissions).
 */
export const addLanguage = <ThrowOnError extends boolean = false>(options: Options<AddLanguageData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AddLanguageResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/languages',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Per-language completeness
 * Return the project's total key count plus, for each language, how many keys are translated, missing, and the completion percentage. Pass `?branch=` to compute the stats against a branch overlay instead of main — see the [branching guide](/docs/guides/branches).
 */
export const getStats = <ThrowOnError extends boolean = false>(options: Options<GetStatsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetStatsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/stats',
        ...options
    });
};

/**
 * List / search keys (paginated)
 * Return a paginated page of a project's translation keys, each with its values across every language. Combine the query params to search by name or source text, filter by tag / status / outdated / completeness, and sort. Pass `?branch=` to read a branch's overlay instead of main — see the [branching guide](/docs/guides/branches).
 */
export const listKeys = <ThrowOnError extends boolean = false>(options: Options<ListKeysData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListKeysResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/keys',
        ...options
    });
};

/**
 * Create a key (write)
 * Add a new key to the project. You may seed its values in one call via the `values` map. On a protected main branch, target a branch with `?branch=` instead — direct main writes are rejected.
 *
 * **Constraints:**
 * - The key must be well-formed: each segment (split on the project's key separator) may contain only letters, digits, `_` and `-`.
 * - Creating a key that already exists returns 409.
 */
export const createKey = <ThrowOnError extends boolean = false>(options: Options<CreateKeyData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateKeyResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/keys',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a key (write)
 * Permanently remove a key and all of its values. On a branch (`?branch=`) the deletion is staged as part of the branch diff and only takes effect on merge. On a protected main branch, direct deletes are rejected — target a branch.
 */
export const deleteKey = <ThrowOnError extends boolean = false>(options: Options<DeleteKeyData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteKeyResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/keys/{keyId}',
        ...options
    });
};

/**
 * Get a key
 * Fetch a single key by id, with its values across all languages. With `?branch=` the branch overlay is returned and keys deleted on the branch are reported as not found.
 */
export const getKey = <ThrowOnError extends boolean = false>(options: Options<GetKeyData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetKeyResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/keys/{keyId}',
        ...options
    });
};

/**
 * Update a key (write)
 * Partial update — omitted fields are left unchanged. On a branch (`?branch=`) the change is staged as a metadata delta rather than applied to main.
 */
export const updateKey = <ThrowOnError extends boolean = false>(options: Options<UpdateKeyData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateKeyResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/keys/{keyId}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Upsert a translation value (write)
 * Set the translation for one key in one language, creating the value if it doesn't exist. If the project requires approval, a write by a non-approver lands as `pending_review`; approvers' writes are auto-approved — see the [approval workflow](/docs/guides/approval-workflow). Pass `?branch=` to stage the change on a branch.
 *
 * **Constraints:**
 * - For a Translator-scoped token, the target language must be within the token owner's assigned languages, or the request is rejected with 403.
 * - For a plural key, pass `pluralForm` (`one`, `other`, …) to target a specific form.
 */
export const upsertKeyValue = <ThrowOnError extends boolean = false>(options: Options<UpsertKeyValueData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<UpsertKeyValueResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/keys/{keyId}/values/{lang}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Import source/translation files (write)
 * Additive import of parsed files. With `?branch=`, values are staged on the branch (no approval workflow) and plural forms are flattened to their `other` form (branch values are single-string).
 *
 * The request body is capped at 10 MB (`bodyLimit`); a larger payload is rejected with 413. Split the files across several calls.
 */
export const importSources = <ThrowOnError extends boolean = false>(options: Options<ImportSourcesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ImportSourcesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/sources',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Export translations as per-language files
 * Render the project's translations to files in the chosen format — one file per language, with each file's contents base64-encoded in `contentBase64`. Use `?languages=` to export a subset and `?status=approved` to ship only approved strings; `?branch=` exports a branch's overlay. See the [import/export guide](/docs/guides/import-export) and the supported [file formats](/docs/reference/file-formats).
 *
 * **Constraints:**
 * - One file is emitted per target language, so the response shape is uniform across single- and multi-language formats.
 */
export const exportTranslations = <ThrowOnError extends boolean = false>(options: Options<ExportTranslationsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ExportTranslationsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/translations',
        ...options
    });
};

/**
 * List branches
 * List the project's translation branches, each with a diff summary against main. Requires branching to be enabled on the project — see the [branching guide](/docs/guides/branches).
 */
export const listBranches = <ThrowOnError extends boolean = false>(options: Options<ListBranchesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListBranchesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches',
        ...options
    });
};

/**
 * Create a branch (write)
 * Create a new branch off main. Branch edits are staged as an overlay and only reach main on merge — see the [branching guide](/docs/guides/branches).
 *
 * **Constraints:**
 * - Requires branching to be enabled on the project.
 * - The branch name must be unique in the project and match the allowed name pattern.
 */
export const createBranch = <ThrowOnError extends boolean = false>(options: Options<CreateBranchData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateBranchResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a branch (write)
 * Permanently delete a branch and discard all of its staged (unmerged) changes. This is a hard delete and cannot be undone; main is unaffected.
 */
export const deleteBranch = <ThrowOnError extends boolean = false>(options: Options<DeleteBranchData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteBranchResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches/{branchId}',
        ...options
    });
};

/**
 * Get a branch (with diff summary)
 * Fetch a single branch by id, including its computed diff summary (keys added/deleted, values modified, languages added/deleted) against main.
 */
export const getBranch = <ThrowOnError extends boolean = false>(options: Options<GetBranchData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetBranchResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches/{branchId}',
        ...options
    });
};

/**
 * List merge conflicts on a branch
 * List the merge conflicts recorded for a branch. Conflicts are populated by [starting a merge](/docs/guides/branches); an unresolved conflict blocks completing the merge.
 */
export const listBranchConflicts = <ThrowOnError extends boolean = false>(options: Options<ListBranchConflictsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListBranchConflictsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches/{branchId}/conflicts',
        ...options
    });
};

/**
 * Resolve merge conflicts (write)
 * Record a resolution for one or more conflicts on this branch. Each resolution picks `main`, `branch`, or a `custom` value. Resolutions are scoped to this branch — a conflict id from another branch is rejected. `customValue` is used only when `resolution` is `custom` (an omitted customValue resolves to an empty string). See the [branching guide](/docs/guides/branches).
 */
export const resolveBranchConflicts = <ThrowOnError extends boolean = false>(options: Options<ResolveBranchConflictsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ResolveBranchConflictsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches/{branchId}/conflicts/resolve',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Start a merge — detect conflicts (write)
 * Begin merging the branch into main. This recomputes and records conflicts against main's current state and reports how many changes will auto-merge. Resolve any returned conflicts, then call merge/complete — see the [branching guide](/docs/guides/branches).
 *
 * **Constraints:**
 * - When the project requires merge approval, personal tokens need the approve permission and machine (project) tokens are rejected.
 */
export const startBranchMerge = <ThrowOnError extends boolean = false>(options: Options<StartBranchMergeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<StartBranchMergeResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches/{branchId}/merge/start',
        ...options
    });
};

/**
 * Complete a merge — apply the branch to main (write)
 * Apply the branch's changes to main and mark the branch merged. All conflicts must be resolved first — otherwise the request fails with 409. Returns a count of what was written to main. See the [branching guide](/docs/guides/branches).
 *
 * **Constraints:**
 * - When the project requires merge approval, personal tokens need the approve permission and machine (project) tokens are rejected.
 */
export const completeBranchMerge = <ThrowOnError extends boolean = false>(options: Options<CompleteBranchMergeData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CompleteBranchMergeResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/branches/{branchId}/merge/complete',
        ...options
    });
};

/**
 * Look up glossary terms
 * Find the glossary terms that occur in one or more source strings, with their approved translations. Batched: pass every source string and every target language in one call.
 *
 * Each match carries `doNotTranslate`, `caseSensitive` and `usageStatus` (`preferred` / `deprecated` / `forbidden`) so a translator — or an agent generating a translation — can honor terminology rules.
 *
 * **Constraints:**
 * - Returns an empty object when the glossary feature is disabled for the org or the project.
 * - At most 100 source texts and 50 languages per request.
 * - Matching is whole-word where the term has word boundaries, substring otherwise (CJK/Thai).
 */
export const lookupGlossaryTerms = <ThrowOnError extends boolean = false>(options: Options<LookupGlossaryTermsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<LookupGlossaryTermsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/glossary/lookup',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Check a translation against the glossary
 * Terminology QA for a proposed translation. Reports where a required term is missing, a forbidden or deprecated term was used, or a do-not-translate term was translated.
 *
 * Severity comes from each glossary's own QA settings, so a rule configured as a warning does not read as an error.
 *
 * **Constraints:**
 * - Returns an empty issue list when the glossary feature is disabled for the org or the project.
 * - Issue kinds: missing_term, forbidden_term, deprecated_term, dnt_violation.
 */
export const checkGlossary = <ThrowOnError extends boolean = false>(options: Options<CheckGlossaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CheckGlossaryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/glossary/check',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * List glossaries
 * List every glossary in the organization, each with its term and language counts. Use this to discover glossary ids for the management endpoints — see the [glossary guide](/docs/guides/glossary).
 */
export const listGlossaries = <ThrowOnError extends boolean = false>(options: Options<ListGlossariesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListGlossariesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/orgs/{orgId}/glossaries',
        ...options
    });
};

/**
 * Create a glossary
 * Create a new glossary in the organization. Its `scope`, approval workflow, and QA severities can be set here or updated later. Add languages and terms in follow-up calls.
 */
export const createGlossary = <ThrowOnError extends boolean = false>(options: Options<CreateGlossaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateGlossaryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/orgs/{orgId}/glossaries',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a glossary
 * Permanently delete the glossary and all of its terms, translations, and language configuration. This cannot be undone.
 */
export const deleteGlossary = <ThrowOnError extends boolean = false>(options: Options<DeleteGlossaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteGlossaryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}',
        ...options
    });
};

/**
 * Get a glossary
 * Fetch a single glossary's settings and metadata by id.
 */
export const getGlossary = <ThrowOnError extends boolean = false>(options: Options<GetGlossaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetGlossaryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}',
        ...options
    });
};

/**
 * Update a glossary
 * Partial update of a glossary's settings — omitted fields are left unchanged. Disabling `approvalRequired` also clears `approvalBypassAdmins`, which is meaningless without it.
 */
export const updateGlossary = <ThrowOnError extends boolean = false>(options: Options<UpdateGlossaryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateGlossaryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * List glossary languages
 * List the languages configured on the glossary, each with its display label.
 */
export const listGlossaryLanguages = <ThrowOnError extends boolean = false>(options: Options<ListGlossaryLanguagesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListGlossaryLanguagesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/languages',
        ...options
    });
};

/**
 * Add a glossary language
 * Add a target language to the glossary so terms can carry a translation in it. The language code is adopted into the org catalog if it isn't already present.
 */
export const addGlossaryLanguage = <ThrowOnError extends boolean = false>(options: Options<AddGlossaryLanguageData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AddGlossaryLanguageResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/languages',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * List glossary terms
 * Return a paginated page of the glossary's terms, each with its per-language translations. Combine the query params to search by text, filter by language, status, or usage, and sort.
 */
export const listGlossaryTerms = <ThrowOnError extends boolean = false>(options: Options<ListGlossaryTermsData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListGlossaryTermsResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/terms',
        ...options
    });
};

/**
 * Create a term
 * Add a term to the glossary. Optionally supply `translations` as a `languageCode -> value` map. When the glossary requires approval, values you cannot approve are recorded as proposals — see the [glossary guide](/docs/guides/glossary).
 *
 * **Constraints:**
 * - Creating a term whose text already exists in the glossary returns 409.
 */
export const createGlossaryTerm = <ThrowOnError extends boolean = false>(options: Options<CreateGlossaryTermData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateGlossaryTermResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/terms',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a term
 * Permanently remove a term and all of its translations from the glossary.
 */
export const deleteGlossaryTerm = <ThrowOnError extends boolean = false>(options: Options<DeleteGlossaryTermData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteGlossaryTermResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/terms/{termId}',
        ...options
    });
};

/**
 * Get a term
 * Fetch a single glossary term by id, with its per-language translations.
 */
export const getGlossaryTerm = <ThrowOnError extends boolean = false>(options: Options<GetGlossaryTermData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetGlossaryTermResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/terms/{termId}',
        ...options
    });
};

/**
 * Update a term
 * Partial update of a term's metadata (text, notes, usage status, flags) — omitted fields are left unchanged. Use the translation endpoint to change values.
 */
export const updateGlossaryTerm = <ThrowOnError extends boolean = false>(options: Options<UpdateGlossaryTermData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateGlossaryTermResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/terms/{termId}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Set a term translation
 * Upsert the translation of a term in one language, creating it if absent. When the glossary requires approval and you cannot approve, the value is stored as a proposal rather than applied — see the [approval workflow](/docs/guides/approval-workflow).
 *
 * **Constraints:**
 * - For a Translator-scoped token, the target language must be within the token owner's assigned languages, or the request is rejected with 403.
 */
export const setGlossaryTermTranslation = <ThrowOnError extends boolean = false>(options: Options<SetGlossaryTermTranslationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<SetGlossaryTermTranslationResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/glossaries/{glossaryId}/terms/{termId}/translations/{lang}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Get fuzzy TM matches
 * Fuzzy-match source strings against the translation memories that apply to this project, returning scored suggestions. Batched: pass every source string and every target language in one call.
 *
 * Each TM's own `minMatchPercentage` and penalty settings (case / whitespace / trailing punctuation) are applied, so `matchPercentage` is the net score after penalties. See the [translation memory guide](/docs/guides/translation-memory).
 *
 * **Constraints:**
 * - Returns an empty object when translation memory is disabled for the org or the project.
 * - At most 100 source texts and 50 languages per request; up to 10 matches per language.
 */
export const getTmMatches = <ThrowOnError extends boolean = false>(options: Options<GetTmMatchesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<GetTmMatchesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/tm/matches',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Concordance search
 * Free-text search across the project's translation memories — *how has this word or phrase been translated before?* — returning the full segments that contain it.
 *
 * This answers what `/tm/matches` cannot: fuzzy matching scores WHOLE segment similarity, so a segment that merely contains your phrase but is otherwise different scores far below threshold and never surfaces. Use `searchIn: "target"` to find where a translated term was used. See the [translation memory guide](/docs/guides/translation-memory).
 */
export const searchTm = <ThrowOnError extends boolean = false>(options: Options<SearchTmData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<SearchTmResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/tm/search',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Pre-translate from TM
 * Apply TM matches to the project's untranslated strings in bulk (leverage). Runs asynchronously — poll the returned `jobId`.
 *
 * Already-translated values are skipped. When the project requires approval, written values follow the caller's approval rights exactly as in the app.
 *
 * **Constraints:**
 * - 409 while another import/export/sync job is running on the project.
 * - `minMatchPercentage` defaults to each TM's configured minimum.
 */
export const pretranslate = <ThrowOnError extends boolean = false>(options: Options<PretranslateData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<PretranslateResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/tm/pretranslate',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Get pre-translate job status
 * Poll the progress of a pre-translate job started via `POST /tm/pretranslate`. Reports live progress and, on completion, how many values were written or skipped.
 */
export const getPretranslateJob = <ThrowOnError extends boolean = false>(options: Options<GetPretranslateJobData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetPretranslateJobResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/tm/pretranslate/{jobId}',
        ...options
    });
};

/**
 * List translation memories
 * List every translation memory in the organization, each with its entry and language counts. This is a management endpoint — call it with a personal access token; project-scoped tokens are refused.
 */
export const listTranslationMemories = <ThrowOnError extends boolean = false>(options: Options<ListTranslationMemoriesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListTranslationMemoriesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/orgs/{orgId}/translation-memories',
        ...options
    });
};

/**
 * Create a translation memory
 * Create a new translation memory in the organization. `minMatchPercentage` (default 75, floor 40) is the threshold below which fuzzy matches from this TM are not surfaced. `penalties` deducts points for case / whitespace / trailing punctuation differences on otherwise-identical sources.
 */
export const createTranslationMemory = <ThrowOnError extends boolean = false>(options: Options<CreateTranslationMemoryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateTranslationMemoryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/orgs/{orgId}/translation-memories',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a translation memory
 * Permanently delete the TM together with all of its entries, translations, and project assignments. This cannot be undone.
 */
export const deleteTranslationMemory = <ThrowOnError extends boolean = false>(options: Options<DeleteTranslationMemoryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteTranslationMemoryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}',
        ...options
    });
};

/**
 * Get a translation memory
 * Fetch a single translation memory by id, including its match settings and penalties.
 */
export const getTranslationMemory = <ThrowOnError extends boolean = false>(options: Options<GetTranslationMemoryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetTranslationMemoryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}',
        ...options
    });
};

/**
 * Update a translation memory
 * Partial update — omitted fields are left unchanged. Set `rebaseSourceData: true` when changing `sourceLanguage` to also move existing entry data onto the new source.
 *
 * **Constraints:**
 * - `approvalBypassAdmins` is meaningless without `approvalRequired` and is forced off when approval is disabled.
 */
export const updateTranslationMemory = <ThrowOnError extends boolean = false>(options: Options<UpdateTranslationMemoryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateTranslationMemoryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * List TM languages
 * List the target languages configured on this translation memory.
 */
export const listTmLanguages = <ThrowOnError extends boolean = false>(options: Options<ListTmLanguagesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListTmLanguagesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/languages',
        ...options
    });
};

/**
 * Add a TM language
 * Add a target language to the translation memory so entries can store translations in it.
 */
export const addTmLanguage = <ThrowOnError extends boolean = false>(options: Options<AddTmLanguageData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AddTmLanguageResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/languages',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * List TM entries
 * Return a paginated page of the TM's source entries, each with its per-language translations. Filter by source text, language, or translation status and sort as needed.
 */
export const listTmEntries = <ThrowOnError extends boolean = false>(options: Options<ListTmEntriesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListTmEntriesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/entries',
        ...options
    });
};

/**
 * Create a TM entry
 * Add a source entry to the TM. Optionally supply `translations` as a `languageCode -> value` map to seed translations in one call. When the TM requires approval, values you cannot approve are recorded as proposals.
 */
export const createTmEntry = <ThrowOnError extends boolean = false>(options: Options<CreateTmEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateTmEntryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/entries',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a TM entry
 * Permanently remove a TM entry and all of its translations.
 */
export const deleteTmEntry = <ThrowOnError extends boolean = false>(options: Options<DeleteTmEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteTmEntryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/entries/{entryId}',
        ...options
    });
};

/**
 * Get a TM entry
 * Fetch a single TM entry by id, with its translations across all languages.
 */
export const getTmEntry = <ThrowOnError extends boolean = false>(options: Options<GetTmEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetTmEntryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/entries/{entryId}',
        ...options
    });
};

/**
 * Update a TM entry
 * Update an entry's source text and/or context. Partial — omitted fields are left unchanged. To edit a translation value, use the entry-translation endpoint instead.
 */
export const updateTmEntry = <ThrowOnError extends boolean = false>(options: Options<UpdateTmEntryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateTmEntryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/entries/{entryId}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Set a TM entry translation
 * Upsert the translation of an entry in one language, creating it if absent. When the TM requires approval and you cannot approve, the value is stored as a proposal on `proposedValue` rather than replacing the live value.
 *
 * **Constraints:**
 * - For a Translator-scoped token, the target language must be within the token owner's assigned languages, or the request is rejected with 403.
 */
export const setTmEntryTranslation = <ThrowOnError extends boolean = false>(options: Options<SetTmEntryTranslationData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).put<SetTmEntryTranslationResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/translation-memories/{tmId}/entries/{entryId}/translations/{lang}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Get the project's AI engine
 * Report which AI engine this project resolves to (its explicit binding, else the org default) and whether AI translation is currently available. Use this before starting a run to confirm AI is set up and to learn how writes will land (`reviewPath`).
 */
export const getAiEngine = <ThrowOnError extends boolean = false>(options: Options<GetAiEngineData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetAiEngineResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/ai/engine',
        ...options
    });
};

/**
 * Translate with AI (write)
 * Machine-translate the project's candidate keys into one or more target languages with the resolved engine. Runs asynchronously — poll the returned `jobId`.
 *
 * Candidates are filtered in SQL before any model sees a string (`onlyEmpty` / `onlyOutdated` / tag filters / `keyPrefix` / explicit `keyIds`). With `?branch=` the run targets a branch instead of main. Writes follow the project's approval workflow exactly as in the app.
 *
 * **Constraints:**
 * - Metered against the org's AI credits when running on a TextSetu platform key; free on a BYOK key.
 * - `autoApprove` is honored only for callers that can approve; otherwise writes land as proposals.
 */
export const aiTranslate = <ThrowOnError extends boolean = false>(options: Options<AiTranslateData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<AiTranslateResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/ai/translations',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Get AI translation job status
 * Poll the progress of a run started via `POST /ai/translations`. Reports live progress and, on completion, how many values were written, skipped, or failed.
 */
export const getAiTranslateJob = <ThrowOnError extends boolean = false>(options: Options<GetAiTranslateJobData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetAiTranslateJobResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/ai/translations/{jobId}',
        ...options
    });
};

/**
 * List subscribable event types
 * The catalog of events an endpoint can subscribe to, with a short description of when each fires.
 */
export const listWebhookEventTypes = <ThrowOnError extends boolean = false>(options?: Options<ListWebhookEventTypesData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListWebhookEventTypesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/webhook-event-types',
        ...options
    });
};

/**
 * List webhook endpoints
 * Every webhook endpoint registered on the project.
 */
export const listWebhooks = <ThrowOnError extends boolean = false>(options: Options<ListWebhooksData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListWebhooksResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhooks',
        ...options
    });
};

/**
 * Create a webhook endpoint
 * Register an HTTPS URL to receive the events you subscribe it to. TextSetu generates a signing secret and returns it once.
 *
 * **Constraints:**
 * - The `secret` is returned ONCE and cannot be retrieved later — store it before discarding the response.
 * - The URL must be publicly reachable — private, loopback and link-local addresses are rejected, and redirects are never followed.
 * - `eventTypes` must contain at least one type from `GET /webhook-event-types`.
 */
export const createWebhook = <ThrowOnError extends boolean = false>(options: Options<CreateWebhookData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<CreateWebhookResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhooks',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Delete a webhook endpoint
 * Remove the endpoint. Its delivery history is removed with it; export anything you need first.
 */
export const deleteWebhook = <ThrowOnError extends boolean = false>(options: Options<DeleteWebhookData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).delete<DeleteWebhookResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhooks/{endpointId}',
        ...options
    });
};

/**
 * Get a webhook endpoint
 * Fetch a single endpoint's configuration and health.
 */
export const getWebhook = <ThrowOnError extends boolean = false>(options: Options<GetWebhookData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetWebhookResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhooks/{endpointId}',
        ...options
    });
};

/**
 * Update a webhook endpoint
 * Change the URL, description, event subscription, or enabled state. The signing secret is untouched — use the rotate endpoint for that.
 *
 * **Constraints:**
 * - Setting `status` to `enabled` also clears the consecutive-failure count, giving a repaired endpoint a full retry budget again.
 */
export const updateWebhook = <ThrowOnError extends boolean = false>(options: Options<UpdateWebhookData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).patch<UpdateWebhookResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhooks/{endpointId}',
        ...options,
        headers: {
            'Content-Type': 'application/json',
            ...options?.headers
        }
    });
};

/**
 * Rotate the signing secret
 * Issue a new signing secret. The previous secret keeps working for a grace period so you can roll over without dropping deliveries.
 *
 * **Constraints:**
 * - The `secret` is returned ONCE and cannot be retrieved later — store it before discarding the response.
 * - During the 24-hour grace window the `webhook-signature` header carries BOTH signatures, space-separated (`v1,<new> v1,<old>`). A compliant verifier accepts a payload if any signature matches.
 */
export const rotateWebhookSecret = <ThrowOnError extends boolean = false>(options: Options<RotateWebhookSecretData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<RotateWebhookSecretResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhooks/{endpointId}/rotate-secret',
        ...options
    });
};

/**
 * Send a test event
 * Queue a `ping` event so you can confirm the endpoint is reachable and your signature verification works before relying on it.
 *
 * **Constraints:**
 * - Delivery is asynchronous — poll the delivery log for the returned `delivery.id` to see the outcome.
 */
export const sendWebhookTest = <ThrowOnError extends boolean = false>(options: Options<SendWebhookTestData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<SendWebhookTestResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhooks/{endpointId}/test',
        ...options
    });
};

/**
 * List webhook deliveries (paginated)
 * The delivery log — every attempt to send an event to an endpoint, with its outcome. Filter by endpoint, event type, or status.
 *
 * **Constraints:**
 * - Response bodies are omitted here; fetch a single delivery to see one.
 */
export const listWebhookDeliveries = <ThrowOnError extends boolean = false>(options: Options<ListWebhookDeliveriesData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<ListWebhookDeliveriesResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhook-deliveries',
        ...options
    });
};

/**
 * Get a webhook delivery
 * A single delivery including the exact payload sent and the endpoint's response.
 *
 * **Constraints:**
 * - Requires manage access rather than read: the response body is content returned by a URL you control, so it is deliberately not exposed to read-only tokens.
 */
export const getWebhookDelivery = <ThrowOnError extends boolean = false>(options: Options<GetWebhookDeliveryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<GetWebhookDeliveryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhook-deliveries/{deliveryId}',
        ...options
    });
};

/**
 * Replay a delivery
 * Re-send a past delivery's original payload — useful after fixing an outage on your side.
 *
 * **Constraints:**
 * - A replay is a NEW delivery with its own `webhook-id`, so a receiver deduping on that header will still process it. The original record is left untouched.
 */
export const replayWebhookDelivery = <ThrowOnError extends boolean = false>(options: Options<ReplayWebhookDeliveryData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).post<ReplayWebhookDeliveryResponse, unknown, ThrowOnError>({
        security: [
            {
                scheme: 'bearer',
                type: 'http'
            }
        ],
        url: '/projects/{projectId}/webhook-deliveries/{deliveryId}/replay',
        ...options
    });
};

/**
 * Fetch a distribution manifest
 * Public, unauthenticated. Returns the current release's manifest — a bare JSON document (NOT the `{success, data}` envelope used elsewhere) listing every published file with an absolute URL and a content hash. Send `If-None-Match` with the previous `ETag` to get a `304`. The manifest is cacheable for a minute; the files it points at are immutable and cacheable forever.
 *
 * **Constraints:**
 * - A `202` means a release is being built — retry after the `Retry-After` interval.
 * - A `404` is returned for an unknown, paused, or never-published distribution, without distinguishing between them.
 */
export const getContentManifest = <ThrowOnError extends boolean = false>(options: Options<GetContentManifestData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<unknown, unknown, ThrowOnError>({
        url: '/content/{publicKey}/manifest.json',
        ...options
    });
};

/**
 * Fetch a published translation file
 * Public, unauthenticated. Serves one content-addressed file from a distribution. The path is taken verbatim from a manifest `files[].url`; it is immutable, so the response is cacheable indefinitely. With a CDN configured these URLs point at object storage and never reach this route.
 */
export const getContentBlob = <ThrowOnError extends boolean = false>(options: Options<GetContentBlobData, ThrowOnError>) => {
    return (options.client ?? _heyApiClient).get<unknown, unknown, ThrowOnError>({
        url: '/content/{publicKey}/b/{blob}',
        ...options
    });
};

/**
 * List the public plan catalog
 * Public, unauthenticated. Returns the published plan catalog with prices, limits, capabilities and retention windows — everything a pricing page needs. Only plans that are on public sale are returned; a plan negotiated for one customer is never included, and there is no parameter that can make it be.
 *
 * **Constraints:**
 * - Currency resolves in this order: `currency` if given, else the currency `country` maps to, else the platform default. The `currency` field in the response is always the one the prices are actually in.
 * - `country` accepts a code (`IN`) or an English name (`India`), so either field of a geolocation response works. It is a HINT — an unrecognized, unmapped, or not-sold-in country falls back to the default currency rather than failing. `currency` is a DEMAND — an unknown or disabled one is a 400.
 * - Plans with no published price in the resolved currency are omitted. If that would empty the catalog, the platform default currency is served instead, so a pricing page never renders blank.
 * - Prices are in MAJOR units of `currency` (dollars, rupees), not minor units. `null` on an overage price means the plan is unmetered for that dimension, not that it is free.
 * - Cacheable for 5 minutes. Send `If-None-Match` with the previous `ETag` to get a `304`.
 */
export const listPublicPlans = <ThrowOnError extends boolean = false>(options?: Options<ListPublicPlansData, ThrowOnError>) => {
    return (options?.client ?? _heyApiClient).get<ListPublicPlansResponse, unknown, ThrowOnError>({
        url: '/plans',
        ...options
    });
};